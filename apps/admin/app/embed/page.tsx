import { ChatWidget } from "../../../widget/components/chat-widget";

export default function EmbedPage() {
  return (
    <main className="min-h-screen">
      <ChatWidget embedded />
    </main>
  );
}
